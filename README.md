Please cd to server-code, and run the following command: sudo docker-compose up -d --build
We used XML parsing library called pugixml. The link of the library is as follows:
https://github.com/zeux/pugixml
